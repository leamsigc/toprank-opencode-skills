---
phase: 04-chrome-devtools-mcp-integration
plan: "01"
type: execute
wave: 1
depends_on: [03-chrome-devtools-cli-integration]
files_modified:
  - seo/seo-analysis/scripts/chrome_audit.py
  - seo/seo-analysis/scripts/detect_js.py
  - seo/seo-analysis/SKILL.md
autonomous: true
requirements: [REQ-SE-01, REQ-SE-02, REQ-SE-03]

must_haves:
  truths:
    - "SEO analysis uses MCP tools (navigate_page, take_snapshot, lighthouse_audit) instead of CLI"
    - "SPA/SSR detection via MCP CDP calls for framework detection"
    - "Headless audit via MCP server with no CLI fallback required"
  artifacts:
    - path: "seo/seo-analysis/scripts/chrome_audit.py"
      provides: "MCP-based page fetching using CDP protocol"
      min_lines: 180
    - path: "seo/seo-analysis/scripts/detect_js.py"
      provides: "MCP-based JS framework detection"
      min_lines: 190
    - path: "seo/seo-analysis/SKILL.md"
      provides: "Updated skill using MCP tools for SEO analysis"
  key_links:
    - from: "chrome_audit.py"
      to: "@modelcontextprotocol/server-chrome-devtools"
      via: "MCP tools (navigate_page, take_snapshot)"
      pattern: "Uses MCP CDP calls instead of CLI subprocess"
    - from: "SKILL.md"
      to: "chrome_audit.py"
      via: "Bash tool with python script"
      pattern: "Phase 0 and Phase 5 execute chrome_audit.py"
---

<objective>
Refactor SEO scripts to use @modelcontextprotocol/server-chrome-devtools MCP instead of CLI subprocess calls. Replace the CLI-first-with-fallback pattern with direct MCP tool usage for headless Chrome page auditing and SPA/SSR detection.

Purpose: Enable seamless execution within MCP-capable environments (Claude Code, opencode.ai) without external CLI dependencies. MCP tools provide better integration and error handling than CLI subprocess calls.

Output: Updated chrome_audit.py, detect_js.py, and SKILL.md using MCP-based CDP calls
</objective>

<context>
@.planning/phases/03-chrome-devtools-cli-integration/03-01-SUMMARY.md
@.mcp.json
@seo/seo-analysis/scripts/chrome_audit.py
@seo/seo-analysis/scripts/detect_js.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor chrome_audit.py to use MCP CDP calls instead of CLI subprocess</name>
  <files>seo/seo-analysis/scripts/chrome_audit.py</files>
  <action>
Refactor the chrome_audit.py script to use MCP-based CDP calls instead of subprocess to chrome-devtools CLI:

1. Remove the chrome-devtools CLI check and subprocess call (lines 33-78)
2. Remove the google-chrome --headless fallback (lines 99-120)
3. Add MCP tool invocation pattern using the CDP protocol directly:
   - Create a function that calls MCP tools via Python's subprocess to npx/mcp
   - Use the @modelcontextprotocol/server-chrome-devtools MCP tools:
     - navigate_page for page navigation with wait-for
     - take_snapshot for HTML capture
   - Output JSON with rendered_html, method='mcp-cdp'

4. Keep the session file reading for port configuration but use MCP protocol

The script should:
- Check for MCP availability via npx
- Use MCP tools: navigate_page + wait-for + take_snapshot
- Fallback to direct CDP WebSocket if MCP unavailable (still better than CLI)
- NO CLI fallback to chrome-devtools or google-chrome --headless
  </action>
  <verify>
    python3 seo/seo-analysis/scripts/chrome_audit.py --url https://example.com --output /tmp/audit_test.json && cat /tmp/audit_test.json | grep -q '"method": "mcp-cdp"'
  </verify>
  <done>
    - chrome_audit.py uses MCP tools (navigate_page, take_snapshot) 
    - Method field shows "mcp-cdp" in output JSON
    - No subprocess calls to chrome-devtools CLI
    - Fallback uses direct CDP WebSocket, not CLI
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor detect_js.py to use MCP CDP calls for framework detection</name>
  <files>seo/seo-analysis/scripts/detect_js.py</files>
  <action>
Refactor detect_js.py to use MCP-based CDP calls for JavaScript framework detection:

1. Add MCP tool invocation to fetch the page via MCP before HTML analysis
2. The script should:
   - Accept an optional --use-mcp flag
   - If MCP enabled, use MCP tools (navigate_page, take_snapshot) to get rendered HTML
   - Then run the existing detect_js_rendering() analysis on the rendered HTML
   - Output JSON includes: requires_js, indicators, confidence, framework, method='mcp-cdp'

3. This enables detection of client-side rendered frameworks that static HTML wouldn't show:
   - React/Vue/Angular apps that render on client
   - Dynamic content loaded via JavaScript
   - Client-side only SPAs

Keep existing static HTML detection as fallback when MCP unavailable.
  </action>
  <verify>
    python3 seo/seo-analysis/scripts/detect_js.py --use-mcp --url https://example.com --output /tmp/detect_test.json && cat /tmp/detect_test.json | grep -q '"method": "mcp-cdp"'
  </verify>
  <done>
    - detect_js.py accepts --use-mcp flag
    - Uses MCP tools to fetch rendered HTML for analysis
    - Method field shows "mcp-cdp" when MCP used
    - Falls back to static HTML analysis when MCP unavailable
  </done>
</task>

<task type="auto">
  <name>Task 3: Update SEO analysis skill to use MCP-based scripts</name>
  <files>seo/seo-analysis/SKILL.md</files>
  <action>
Update the SEO analysis skill (SKILL.md) to use the refactored MCP-based scripts:

1. In Phase 0 (Technical Audit), update the chrome_audit.py invocation:
   - Remove any mention of chrome-devtools CLI fallback
   - The script now uses MCP tools automatically
   - Add note about MCP requirement for full JavaScript rendering

2. In Phase 5 (Content Audit), add --use-mcp flag to detect_js.py:
   - Enables MCP-based JS framework detection
   - Captures client-side rendered content analysis

3. Update prerequisites section:
   - Requires @modelcontextprotocol/server-chrome-devtools MCP server
   - chrome-devtools CLI no longer required (MCP provides equivalent functionality)

4. Update the tool references to reflect MCP-only execution
  </action>
  <verify>
    grep -q "MCP" seo/seo-analysis/SKILL.md && grep -q "@modelcontextprotocol/server-chrome-devtools" seo/seo-analysis/SKILL.md
  </verify>
  <done>
    - SKILL.md references MCP-based execution
    - Prerequisites include @modelcontextprotocol/server-chrome-devtools
    - No mention of chrome-devtools CLI as requirement
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script → MCP server | Local subprocess calls to npx, no network to external services |
| MCP → Chrome CDP | Local WebSocket to Chrome debugging port |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | chrome_audit.py script modification | mitigate | Script runs locally with user-provided URL only |
| T-04-02 | Information Disclosure | URLs passed to CDP | accept | Public URLs only; no credentials in URL parameters |
| T-04-03 | Denial of Service | MCP/Chrome timeout handling | mitigate | Timeout handling already present in script |
</threat_model>

<verification>
- Task 1: chrome_audit.py outputs method='mcp-cdp' in JSON
- Task 2: detect_js.py outputs method='mcp-cdp' when --use-mcp used
- Task 3: SKILL.md updated with MCP references and no CLI fallback
</verification>

<success_criteria>
- SEO analysis uses MCP tools instead of CLI subprocess
- SPA/SSR detection via MCP CDP calls 
- Headless audit via MCP server (no CLI fallback required)
- Requirements REQ-SE-01, REQ-SE-02, REQ-SE-03 addressed
</success_criteria>

<output>
After completion, create .planning/phases/04-chrome-devtools-mcp-integration/04-01-SUMMARY.md
</output>