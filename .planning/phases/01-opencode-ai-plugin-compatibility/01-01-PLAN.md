---
phase: "01-opencode-ai-plugin-compatibility"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - ".opencode/config.json"
  - ".opencode/plugin.ts"
  - ".opencode/skills/index.ts"
autonomous: true
requirements:
  - "REQ-PC-01"
  - "REQ-PC-02"
  - "REQ-PC-03"
user_setup: []

must_haves:
  truths:
    - ".opencode/ directory exists with valid plugin configuration"
    - "Plugin can be loaded by opencode.ai runtime"
    - "Skills are discoverable via plugin system"
  artifacts:
    - path: ".opencode/config.json"
      provides: "Plugin manifest with metadata"
      contains: "name, version, skills"
    - path: ".opencode/plugin.ts"
      provides: "Main plugin entry point"
      contains: "export default plugin"
  key_links:
    - from: ".opencode/config.json"
      to: ".opencode/plugin.ts"
      via: "import in main field"
---

<objective>
**Phase 1 Plan 1:** Create `.opencode/` directory structure with plugin entry point

Purpose: Establish the opencode.ai plugin structure matching the existing `.claude-plugin/` pattern but compatible with opencode.ai's plugin system.

Output: Working `.opencode/` directory with TypeScript plugin
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Current project structure - reference existing patterns
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .opencode/ directory structure</name>
  <files>.opencode/config.json</files>
  <action>
Create `.opencode/config.json` with plugin manifest:

```json
{
  "name": "toprank",
  "version": "1.0.0",
  "description": "Toprank - SEO + Google Ads plugin for Claude Code",
  "main": "plugin.ts",
  "skills": [
    { "name": "ads", "path": "./skills/ads" },
    { "name": "seo-analysis", "path": "./skills/seo-analysis" }
  ],
  "dependencies": {
    "@opencode-ai/plugin": ">=0.1.0"
  }
}
```

Match structure from `.claude-plugin/plugin.json` where possible for dual-format coexistence (REQ-PC-03).
  </action>
  <verify>
    <automated>test -f .opencode/config.json && echo "exists" || echo "missing"</automated>
  </verify>
  <done>config.json exists with valid JSON, contains name, version, main fields</done>
</task>

<task type="auto">
  <name>Task 2: Create plugin.ts entry point</name>
  <files>.opencode/plugin.ts</files>
  <action>
Create main plugin file using `@opencode-ai/plugin` SDK pattern:

```typescript
import { Plugin, Skill } from '@opencode-ai/plugin';

// Import skills from existing .claude-plugin/ structure
const skills: Skill[] = [
  // Map existing skills to opencode format
];

export const plugin: Plugin = {
  name: 'toprank',
  version: '1.0.0',
  skills,
  hooks: {
    onLoad: async () => {
      console.log('Toprank plugin loaded');
    },
  },
};

export default plugin;
```
  </action>
  <verify>
    <automated>test -f .opencode/plugin.ts && npx tsc --noEmit .opencode/plugin.ts 2>/dev/null || echo "skip"</automated>
  </verify>
  <done>plugin.ts exists, exports default plugin object</done>
</task>

<task type="auto">
  <name>Task 3: Create skills index</name>
  <files>.opencode/skills/index.ts</files>
  <action>
Create skills registry that maps existing `.claude-plugin/` skills to opencode format:

```typescript
import { Skill } from '@opencode-ai/plugin';
import { adsSkill } from './ads';
import { seoSkill } from './seo-analysis';

export const skills: Skill[] = [adsSkill, seoSkill];
export { adsSkill, seoSkill };
```
  </action>
  <verify>
    <automated>test -d .opencode/skills && ls .opencode/skills/ | head -1</automated>
  </verify>
  <done>.opencode/skills/ directory exists with skill exports</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→plugin | Untrusted user input passes through skill invocations |
| config→runtime | Plugin config loaded before user code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | config.json | mitigate | Validate JSON schema on load, reject invalid configs |
| T-01-02 | Repudiation | plugin.ts | accept | No sensitive operations in plugin entry |
</threat_model>

<verification>
- [ ] `.opencode/config.json` exists and is valid JSON
- [ ] `.opencode/plugin.ts` exports default plugin
- [ ] Skills can be imported from `.opencode/skills/`
</verification>

<success_criteria>
- [ ] User can install Toprank plugin in opencode.ai plugin directory
- [ ] Skills are discovered and listed by opencode.ai plugin system
- [ ] No "opencode-openai-codex-auth" pattern in plugin code
</success_criteria>

<output>
After completion, create `.planning/phases/01-opencode-ai-plugin-compatibility/01-01-SUMMARY.md`
</output>