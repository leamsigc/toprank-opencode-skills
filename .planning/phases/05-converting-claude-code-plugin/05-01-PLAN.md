# Phase 5: Convert Claude Code Plugin - Plan

**Phase:** 5
**Goal:** Convert existing Claude Code plugin (`.claude-plugin/`) to opencode.ai native skills (`.opencode/`) with full feature parity
**Created:** 2026-04-16
**Status:** Ready for execution

---

## Context Summary

- **All 10 skills** must be converted from `.claude-plugin/plugin.json` to `.opencode/plugin.ts`
- **Dual format** coexistence maintained (`.claude-plugin/` + `.opencode/`)
- **MCP tool patterns** (`mcp__adsagent__*`) replaced with native opencode.ai tool calling

---

## Tasks

### 5.01: Update plugin.ts with all 10 skills

**Task:** Expand `.opencode/plugin.ts` to include all 10 skills from `.claude-plugin/plugin.json`

```typescript
const skills: Skill[] = [
  { name: 'ads', path: './google-ads/ads', triggers: ['/toprank:ads', 'google ads', 'campaigns', 'keywords'] },
  { name: 'ads-audit', path: './google-ads/ads-audit', triggers: ['/toprank:ads-audit', 'ads audit'] },
  { name: 'ads-copy', path: './google-ads/ads-copy', triggers: ['/toprank:ads-copy', 'ad copy'] },
  { name: 'seo-analysis', path: './seo/seo-analysis', triggers: ['/toprank:seo-analysis', 'seo analysis', 'SEO audit'] },
  { name: 'content-writer', path: './seo/content-writer', triggers: ['/toprank:content-writer', 'content writer'] },
  { name: 'keyword-research', path: './seo/keyword-research', triggers: ['/toprank:keyword-research', 'keyword research'] },
  { name: 'meta-tags-optimizer', path: './seo/meta-tags-optimizer', triggers: ['/toprank:meta-tags-optimizer', 'meta tags'] },
  { name: 'schema-markup-generator', path: './seo/schema-markup-generator', triggers: ['/toprank:schema-markup-generator', 'schema markup'] },
  { name: 'setup-cms', path: './seo/setup-cms', triggers: ['/toprank:setup-cms', 'setup cms'] },
  { name: 'gemini', path: './gemini', triggers: ['/toprank:gemini', 'gemini'] },
];
```

**Verify:** All 10 skill entries present in plugin.ts

---

### 5.02: Create skill directories in .opencode/

**Task:** Create skill directory structure for opencode.ai if different from root

- Check if skills need to be copied to `.opencode/skills/` or can reference root directories
- Verify path resolution works for both platforms

**Verify:** `ls .opencode/skills/` shows directories matching skill names

---

### 5.03: Replace MCP tool patterns with native tool calling

**Task:** Update skill implementations to use opencode.ai native tool calling instead of `mcp__adsagent__*` patterns

**Files to update:**
- `google-ads/ads/SKILL.md` — Replace mcp__adsagent__ calls
- `google-ads/ads-copy/SKILL.md` — Replace mcp__adsagent__ calls
- `google-ads/shared/preamble.md` — Update MCP detection logic

**Replace patterns:**
```
mcp__adsagent__getAccountInfo  →  tool("getAccountInfo", { accountId })
mcp__adsagent__listCampaigns   →  tool("listCampaigns", { accountId })
```

**Verify:** No `mcp__adsagent__` strings remain in skills (grep check)

---

### 5.04: Test plugin loading in opencode.ai

**Task:** Verify the plugin loads and skills are discovered

- Run test command to load plugin
- Verify all 10 skills appear in available skills list

**Verify:** `opencode.ai` shows all 10 skills when plugin loads

---

### 5.05: Verify feature parity

**Task:** Ensure all features work identically in both environments

- Test each skill trigger phrase
- Verify same outputs in opencode.ai as in Claude Code

**Verify:** All 10 skills respond correctly to their triggers

---

## Dependencies

| Task | Depends On |
|------|-----------|
| 5.02 | 5.01 |
| 5.03 | 5.01 |
| 5.04 | 5.02, 5.03 |
| 5.05 | 5.04 |

---

## Success Criteria

- [ ] All 10 skills defined in plugin.ts
- [ ] Skills work in opencode.ai environment
- [ ] No feature loss vs current implementation
- [ ] MCP patterns replaced with native tool calling
- [ ] Dual-format coexistence maintained

---

*Plan: 05-01*
*Created: 2026-04-16*